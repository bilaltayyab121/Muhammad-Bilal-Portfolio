# Optional. Leave blank in dev — Vite proxies /api → http://localhost:5000
# In production, set this to the deployed backend base URL, e.g.:
# VITE_API_URL=https://api.yourdomain.com/api
VITE_API_URL=
